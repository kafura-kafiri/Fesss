restaurant is new what to do when data is not enough.
save(): -> store models in to unix file system.
/feed :restaurant data.
/get :type, expected, _date, __y__.


/feed > reform data > when() to fit > then fit save().
/get > get() > predict().